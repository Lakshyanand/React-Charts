import React from "react";

const Header = () => {
    return(
        <header>
            <nav style={{backgroundColor:'black', padding: '10px', color:'white', fontWeight: '800'}}>
                GDP Charts
            </nav>
        </header>
    )
};

export default Header;